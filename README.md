# Computer-Prototype-378

## Build and Run Instructions ##

### MacOS, Linux, BSD, or other Unix-like OSes...
`cd client/`
`npm install`
`npm run dev &`
`cd ..`
`dotnet build`
`dotnet run`
Then navigate to `http://localhost:5173/` in your web browser!

### Windows
`cd client`
`npm install`
`Start-Process npm run dev`
`cd ../`
`dotnet build`
`dotnet run`
Then navigate to `http://localhost:5173/` in your web browser!

## Tasks to be Completed ##

### Task 1 ###
User will be able to set a weight goal and view their progress at any given point.

### Task 2 ###
User will be able to view the caloric value of meal items.

### Task 3 ###
User will be able to set preferences so that allergies and dislikes are handled properly.

## Main Feature ##
User will be able to retrieve meals from a local database.

